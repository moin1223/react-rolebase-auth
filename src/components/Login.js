import React from 'react'
import useAuth from '../hooks/useAuth'

const Login = () => {
  // const { setAuth } = useAuth();
  // const user = {"roles":["1"], "accessToken":"gHhgH"}
  // setAuth(user)
  return (
    <div>Login</div>
  )
}

export default Login